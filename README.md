# SkyFitnessPro — Дипломный проект

## 📖 Описание

Веб-приложение для фитнес-платформы, позволяющее пользователям просматривать курсы, проходить тренировки, отслеживать прогресс и управлять своим профилем. Разработано в рамках дипломного проекта.

## 🛠 Стек технологий

- Frontend: Next.js (Pages Router), React, TypeScript
- Стилизация: CSS Modules + styled-components
- База данных: MongoDB (драйвер `mongodb`) — локально
- Аутентификация:
    - JWT-токены (фитнес-API), localStorage, React Context
    - Внешний API: `https://wedev-api.sky.pro/api/user` (профиль)
    - Внешний API: `https://wedev-api.sky.pro/api/fitness` (курсы)

- Работа с данными:
    - Курсы/тренировки/прогресс: `https://wedev-api.sky.pro/api/fitness`
    - Пользователи/профиль: `https://wedev-api.sky.pro/api/user`

- Особенность: Запросы к учебному API должны отправляться **без заголовка** `Content-Type: application/json`
- Деплой: Cloudflare Pages (настройка в процессе)
- Инструменты: Git, npm, axios (для авторизации), fetch (для курсов)

## ✅ Реализованный функционал

- [x] Маршрутизация и защита страниц (Middleware)
- [x] Динамические страницы курсов (`/courses/[id]`)
- [x] API-роуты для работы с курсами, тренировками, пользователями и прогрессом
- [x] TypeScript-интерфейсы для типизации данных (без `any`)
- [x] Полноценная авторизация: вход, регистрация, выход, сохранение токена
- [x] **Двойная система авторизации** (профиль + фитнес-API с JWT)
- [x] **Валидация пароля по требованиям фитнес-API** (мин. 6 символов, заглавная буква, 2 спецсимвола)
- [x] Модальные окна авторизации поверх главной страницы (через query-параметры)
- [x] Многоуровневая валидация: клиентская (regex, пустые поля) + серверная
- [x] Безопасная обработка ошибок в соответствии со стандартами OWASP
- [x] Разделение клиентов для разных внешних API (курсы ↔ авторизация)
- [x] Fallback на моковые данные при недоступности внешнего API
- [x] Интеграция с MongoDB (локальная разработка)
- [x] **Профиль пользователя** с отображением добавленных курсов
- [x] **Отслеживание прогресса тренировок** с визуализацией
- [x] **Интерактивные карточки курсов** (кнопка + / - с сохранением состояния)
- [x] **Кастомный курсор** при наведении на кнопки курсов
- [x] **Адаптивные аватары** с fallback при битых ссылках
- [x] Настройка Cloudflare Pages (базовый деплой)

## 📁 Структура проекта
### SkyFitnessPro---diploma-thesis/

├── pages/ # Маршруты и API-роуты
├── components/ # Переиспользуемые UI-компоненты
│ ├── CourseCard/ # Карточка курса с кнопкой добавления
│ ├── MyCourses/ # Компонент списка курсов в профиле
│ ├── Header/ # Шапка с аватаром и меню
│ ├── Profile/ # Страница профиля
│ └── auth/ # Модалки входа/регистрации
├── libs/ # Утилиты, работа с БД, API-клиент
├── context/ # React Context (AuthContext)
├── types/ # TypeScript интерфейсы
├── public/ # Статические файлы (изображения, иконки)
└── package.json # Зависимости и скрипты

## 🔐 Архитектура авторизации
### Архитектура двойной авторизации

Проект использует **две разные системы авторизации**, так как фитнес-API не возвращает данные пользователя (имя, аватар, ID), а основной API не выдаёт JWT-токены для работы с курсами.

### Процесс входа/регистрации:

1. **Основной API** (`/api/user/login`) — получаем данные пользователя (имя, аватар, `_id`)
2. **Фитнес-API** (`/api/fitness/auth/login`) — получаем JWT-токен для курсов
3. **Сохраняем в localStorage**: данные пользователя из первого, JWT-токен из второго

| API                     | Назначение           | Что возвращает                                                |
| ----------------------- | -------------------- | ------------------------------------------------------------- |
| Основной (`/api/user`)  | Профиль пользователя | `_id`, `login`, `name`, `imageUrl`, `token` (не используется) |
| Фитнес (`/api/fitness`) | Курсы и прогресс     | JWT-токен (`eyJ...`) для авторизованных запросов              |

### Два базовых URL для разных типов запросов
| Тип запроса                    | Базовый URL                             | Файл-клиент         | Особенности                                      |
| ------------------------------ | --------------------------------------- | ------------------- | ------------------------------------------------ |
| 📚 Курсы, тренировки, прогресс | `https://wedev-api.sky.pro/api/fitness` | `libs/apiConfig.ts` | Использует `fetch`, заголовок `Content-Type: ""` |
| 🔐 Авторизация, профиль        | `https://wedev-api.sky.pro/api`         | `libs/apiAuth.ts`   | Использует `axios`, заголовок `Content-Type: ""` |

### ⚠️ Важное требование API
Учебный сервер **не принимает** заголовок `Content-Type: application/json`. Все запросы должны отправляться либо с пустым заголовком, либо без него вовсе:

```typescript
// Правильно:
headers: { "Content-Type": "" }

// Неправильно (вернёт 400 ошибку):
headers: { "Content-Type": "application/json" }
```

### 🔑 Требования к паролю (фитнес-API)
Фитнес-API предъявляет строгие требования к паролю. На клиенте реализована функция validateFitnessPassword, которая проверяет пароль до отправки на сервер:
Минимум 6 символов
Минимум 1 заглавная буква (латиница или кириллица)
Минимум 2 спецсимвола (например: @!#?)
Примеры:

❌ password123 — нет заглавной, нет спецсимволов
❌ Password123 — нет спецсимволов
✅ Password@! — соответствует всем требованиям

### 📁 Новые файлы для авторизации
libs/
├── apiConfig.ts # Клиент для курсов (fetch) + JWT-интерцептор
├── apiAuth.ts # Клиент для авторизации (axios)
├── fitness.ts # Локальные функции для MongoDB
└── mockCourses.ts # Моковые данные для фоллбэка

## 👤 Профиль пользователя и курсы
### Отображение добавленных курсов
Компонент MyCourses загружает курсы пользователя с сервера через GET /api/fitness/users/me и отображает их в виде карточек с прогрессом:

- Загрузка списка ID курсов из response.user.selectedCourses
- Параллельная загрузка данных каждого курса через Promise.all
- Получение прогресса через GET /users/me/progress?courseId={id}
- Визуализация прогресса — анимированная полоса с градиентом

### Интерактивные карточки курсов
На главной странице и в профиле карточки курсов имеют интерактивные кнопки:

- Главная страница: кнопка + / - для добавления/удаления курса
- Страница курса: кнопка "Добавить курс" / "Курс уже добавлен ✓"
- Сохранение состояния: при перезагрузке страницы состояние кнопки восстанавливается через проверку /users/me
- Кастомный курсор: при наведении на кнопку появляется кастомный курсор с текстом действия

### Аватары пользователей
Система аватаров с многоуровневым fallback:

- В хедере: /img/icon.png (маленький аватар)
- В профиле: /img/avatar.png (большой аватар)
- При загрузке фото: отображается в обоих местах
- Обработка битых ссылок: если imageUrl содержит битую ссылку с yandexcloud — показывается fallback

## 🎨 Страницы авторизации и регистрации
### Архитектура модалок
Формы входа и регистрации реализованы как **модальные окна поверх главной страницы**, а не как отдельные маршруты. Это обеспечивает:

- Плавный UX без перезагрузки страницы
- Сохранение состояния курсов и фильтров
- Единый визуальный контекст приложения

### Механизм работы:
- Ссылки в шапке ведут на `/?modal=login` и `/?modal=register`
- Главная страница (`pages/index.tsx`) отслеживает query-параметр `modal` через `useRouter`
- При наличии параметра рендерится соответствующий компонент модалки поверх контента
- Закрытие окна (клик по затемнению или успешная авторизация) возвращает пользователя в исходное состояние через `shallow routing`

### Многоуровневая валидация
Валидация данных происходит на двух уровнях:

**1. Клиентская (мгновенная обратная связь)**
- Пустые поля — проверка `trim()` перед отправкой
- Формат email — регулярное выражение `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
- Совпадение паролей — проверка идентичности в форме регистрации
- Требования фитнес-API — проверка сложности пароля через validateFitnessPassword
- Визуальная индикация — красная рамка на невалидном поле (`styles.inputError`)
- Сброс ошибки — при начале ввода в подсвеченном поле
- Отключение браузерной валидации — атрибут `noValidate` на форме

**2. Серверная (безопасность и бизнес-логика)**
- Проверка существования пользователя в БД
- Валидация сложности пароля
- Защита от дублирования записей

**3. Обработка "двойных" ошибок**
- Если курс уже добавлен — показывается сообщение "Курс уже в вашем профиле" (не ошибка)
- Если пароль не соответствует требованиям — понятное сообщение на русском

### Безопасность обработки ошибок
Внешний учебный API (`wedev-api.sky.pro`) реализует **безопасную обработку ошибок авторизации**: для предотвращения перебора зарегистрированных email-адресов сервер возвращает **единый статус 400** без уточнения причины (почта не найдена / неверный пароль / пользователь уже существует).

На клиенте реализована соответствующая унифицированная обработка:

- При любой ошибке авторизации показывается сообщение: _"Неверные данные. Проверьте почту и пароль"_
- Подсвечивается поле email как наиболее вероятная причина
- Это соответствует современным стандартам безопасности (OWASP)

### Защита от повторной отправки
- Кнопка отправки блокируется через `disabled={isLoading}` во время запроса
- Предотвращает дублирование запросов к серверу
- Визуально показывает процесс загрузки ("Вход..." / "Регистрация...")

### Файлы модалок
| Файл                                    | Назначение                                           |
| --------------------------------------- | ---------------------------------------------------- |
| `components/auth/LoginModal.tsx`        | Модальное окно входа                                 |
| `components/auth/RegisterModal.tsx`     | Модальное окно регистрации                           |
| `components/auth/StyleModal.module.css` | Стили модалок и валидации                            |
| `context/AuthContext.tsx`               | React Context для управления состоянием пользователя |
| `libs/apiAuth.ts`                       | API-клиент для авторизации (axios)                   |

## 🚀 Локальный запуск
1. Установите зависимости:

```bash
    npm install
```
2. Запустите локальную MongoDB (порт 27017).
3. Создайте файл .env.local в корне проекта:
    - MONGODB_URI=mongodb://127.0.0.1:27017/skyfitness
    - MONGODB_DB=skyfitness
    - JWT_SECRET=your_secret_key_here

4. Запустите сервер разработки:
```bash
    npm run dev
```
5. Откройте http://localhost:3000

## 🧪 Тестирование функционала
### Проверка работы авторизации
**1. Регистрация нового пользователя:**
- Email: jora18@yandex.ru
- Пароль: Secure@! (соответствует требованиям)

**2. Проверка JWT-токена в консоли браузера:**
- const token = localStorage.getItem("fitness_token");
- console.log("Токен:", token);
- console.log("Начинается с eyJ?", token?.startsWith("eyJ"));

**3. Добавление курса — кнопка "+" должна смениться на "-"**
**4. Перезагрузка страницы (F5) — состояние кнопки должно сохраниться**
**5. Профиль — курс должен отобразиться с прогрессом**

### 🧪 Возможные проблемы и решения
**1. `Error: Невалидный токен`**
- Причина: В localStorage лежит старый токен
- Решение: Выполнить `localStorage.clear()` и войти заново

**2. Пользователь с таким логином уже существует**
- Причина: Уже зарегистрирован в основном API
- Решение: Войти с существующими данными

**3. Пользователь с таким email не найден**
- Причина: Не зарегистрирован в фитнес-API
- Решение: Зарегистрироваться заново

**4. Кнопка `+` не меняется на `-` после F5**
- Причина: Не срабатывает проверка `/users/me`
- Решение: Проверить, что токен начинается с `eyJ`

**5. Аватар не отображается**
- Причина: Ссылка `imageUrl` битая (двойное кодирование)
- Решение: Срабатывает fallback на `/img/icon.png`

**6. Курс не отображается в профиле**
- Причина: Неправильно читаем `response.user`
- Решение: Использовать `response.user.selectedCourses`

**7. Пароль не принимается**
- Причина: Не соответствует требованиям фитнес-API
- Решение: Пароль с заглавной буквой и 2+ спецсимволами


## 🌐 Деплой
### Проект настроен для развёртывания на Cloudflare Pages.
- Примечание: На текущем этапе деплой находится в стадии тестирования. Основная разработка ведётся локально.

## 📋 Планы развития (TODO)
- Реализация отображения тренировок внутри курса
- Функционал отметки прогресса тренировок (сохранение в API)
- Настройка ESLint + Prettier
- Написание тестов (Jest + React Testing Library)
- Финальная настройка и оптимизация деплоя
- Рефакторинг: объединение двух систем авторизации (когда фитнес-API начнёт возвращать данные пользователя)

## 👤 Автор
Анна Морозова (AnnaM-7777777) | Дипломный проект, 2026
